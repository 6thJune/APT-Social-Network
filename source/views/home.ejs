<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://site-assets.fontawesome.com/releases/v6.7.2/css/all.css">
    <link rel="stylesheet" href="/styles/general.css">
    <link rel="stylesheet" href="/styles/pages.css">
    <script src="/scripts/general.js" defer></script>
    <script src="/scripts/pages.js" defer></script>
    <title>APT Trang chủ</title>
</head>
<body>
    <div id="page-loader">
        <img src="/images/page-loader.gif" alt="">
    </div>
    <div id="top" style="position: absolute; top: 0;"></div>
    <div class="left-silde">
        <a href="#top"><img src="/images/APT-logo.png" alt="" class="left-silde__main-logo"></a>
        <div class="left-side__menu">
            <div class="menu__item --selected"><i class="fa-regular fa-house"></i>Trang chủ</div>
            <div class="menu__item" onclick="location.href='/profile'"><i class="fa-regular fa-user"></i>Trang cá nhân</div>
        </div>
    </div>
    <div class="search-bar">
        <input class="search-bar__input" type="search" placeholder="Tên đăng nhập">
        <i class="fa-regular fa-magnifying-glass search-bar__icon"></i>
        <div class="search-bar__result"></div>
    </div>
    <div class="feed --home-feed">
        <% if(posts.length == 0) { %>
        <p class="feed-empty-message-title">CHƯA CÓ BÀI VIẾT</p>
        <p class="feed-empty-message-describe">Đến <a href="/profile">Trang cá nhân</a> để đăng bài!</p>
        <% } %>
        <% for (let i = posts.length - 1; i >= 0; i--) { %>
            <div class="feed__post" post-id="<%= posts[i]._id %>">
                <div class="post__head">
                    <div class="head__auth-name"><%= posts[i].username %></div>
                </div>
                <div class="post__time"><%= posts[i].time %></div>
                <div class="post__content"><%= posts[i].content %></div>
                <img class="post__photo" src="<%= posts[i].photoUrl %>" alt="">
                <i class="<%= currentUser.postsLiked.includes(posts[i]._id)?'fa-solid':'fa-regular' %> fa-heart post__react" onclick="likePost(this)"></i>
                <p class="post__no-like"><%= posts[i].noLike %> lượt thích</p>
            </div>
        <% } %>
    </div>
</body>
</html>